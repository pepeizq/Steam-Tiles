<Page
    x:Class="Steam_Tiles_for_Windows_10.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Steam_Tiles_for_Windows_10"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d" Loading="Page_Loading">

    <Page.Resources>
        <DataTemplate x:Key="dataTemplateTilesSteam">
            <Grid Width="188" Height="87" Margin="1">
                <Image Source="{Binding Path=imagen}" ToolTipService.ToolTip="{Binding Path=titulo}" />
            </Grid>
        </DataTemplate>
    </Page.Resources>

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <Border>
            <Border.Background>
                <LinearGradientBrush StartPoint="0.5,0" EndPoint="0.5,1">
                    <GradientStop Color="Gray" Offset="0.5" />
                    <GradientStop Color="LightGray" Offset="1.0" />
                </LinearGradientBrush>
            </Border.Background>
        </Border>

        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <Grid Grid.Row="0" Background="#FFE4E4E4" >
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>

                <TextBlock x:Name="tbDirectorioSteam" Grid.Row="0" x:Uid="Directorio" Margin="10" />

                <Grid Grid.Row="1" Margin="10">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>

                    <Button x:Name="buttonBuscarRutaTilesSteam" Grid.Column="0" x:Uid="Boton Busqueda" Padding="10,5" Margin="0,0,10,0" BorderBrush="DarkGray" Background="Transparent" />
                    <TextBox x:Name="tbRutaTilesSteam" Grid.Column="1" IsSpellCheckEnabled="False" IsReadOnly="True" VerticalAlignment="Center"  />
                </Grid>

                <Grid x:Name="gridCarpeta2" Grid.Row="2" Margin="10" Visibility="Collapsed">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>

                    <Button x:Name="buttonBuscarRutaTilesSteam2" Grid.Column="0" x:Uid="Boton Busqueda" Padding="10,5" Margin="0,0,10,0" BorderBrush="DarkGray" Background="Transparent" />
                    <TextBox x:Name="tbRutaTilesSteam2" Grid.Column="1" IsSpellCheckEnabled="False" IsReadOnly="True" VerticalAlignment="Center"  />
                </Grid>

                <Grid Grid.Row="3" Margin="10,0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>
                                       
                    <CheckBox x:Name="checkboxTilesSteamTitulo" Grid.Column="0" x:Uid="Titulo Tile" Margin="0,10,10,10" FlowDirection="RightToLeft" />
                    <Button x:Name="buttonVotaciones" Grid.Column="1" x:Uid="Boton Votar" Padding="10,5" BorderBrush="DarkGray" Background="Transparent" />
                    <!--<Button x:Name="buttonDonaciones" Grid.Column="2" x:Uid="Boton Donar" Padding="10,5" BorderBrush="DarkGray" Background="Transparent" />-->
                </Grid>
            </Grid>

            <Grid x:Name="gridTilesSteam" Margin="10" Grid.Row="1" HorizontalAlignment="Center">
                <ScrollViewer x:Name="scrollViewerGridSteam" VerticalScrollMode="Auto" Visibility="Collapsed" >
                    <GridView x:Name="gridViewTilesSteam" ItemTemplate="{StaticResource dataTemplateTilesSteam}" IsItemClickEnabled="True" Padding="0,0,15,0" />
                </ScrollViewer>
            </Grid>

            <Grid x:Name="gridAviso" Margin="10" Grid.Row="2" HorizontalAlignment="Center" Background="DarkRed" Visibility="Collapsed">
                <TextBlock x:Name="textBlockAviso" Foreground="White" Padding="10" />
            </Grid>
        </Grid>
       
    </Grid>
</Page>


